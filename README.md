# building / running
```bash
# development shell using nix-shell:
nix-shell
# development shell using nix flakes:
nix develop
# development shell using direnv
direnv allow
# using lorri, force compilation
lorri watch --once
```
